<Window x:Class="DotaUpcomingEventsTicker.LiveMatchDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LiveMatchDetails" 
        Height="220" 
        Width="350"
        BorderThickness="1"
        BorderBrush="Green"
        WindowStyle="None"
        ResizeMode="NoResize">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button Style="{StaticResource MaterialDesignFlatButton}" 
                    Padding="0" Margin="0,0,10,0" 
                    Command="{Binding OnCloseNotificationCommand}"
                    CommandParameter="{Binding Path=Match}"
                    Height="20">CLOSE</Button>
        </StackPanel>
    </Grid>
</Window>
